import { BadgeCheck, Camera, Check, LockKeyhole, ScanFace, Star } from 'lucide-react'
import { PageShell } from './page-shell'
import type { ScanState } from '@/lib/estacionando/types'

export function Profile({ verified, scanState, startScan }: { verified: boolean; scanState: ScanState; startScan: () => void }) {
  return <PageShell eyebrow="Tu cuenta" title="Perfil y confianza" description="Tu identidad verificada hace que compartir espacios sea más seguro para todos."><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-3xl bg-primary p-7 text-primary-foreground"><div className="flex size-16 items-center justify-center rounded-2xl bg-accent text-2xl font-semibold text-accent-foreground">LT</div><h2 className="mt-5 text-2xl font-semibold">Lorenzo Teixido</h2><p className="mt-1 text-sm text-muted-foreground">Miembro desde 2026 · Santiago</p><div className="mt-8 border-t border-border pt-5"><p className="text-xs text-muted-foreground">Tu reputación</p><div className="mt-3 flex items-center gap-2"><Star className="size-4 fill-accent text-accent" /><b>Nuevo miembro</b></div></div></div><div className="rounded-3xl border border-border bg-card p-6 sm:p-7"><div className="flex items-start justify-between gap-4"><div><span className="inline-flex size-11 items-center justify-center rounded-2xl bg-accent/15 text-accent"><ScanFace className="size-5" /></span><h2 className="mt-5 text-2xl font-semibold tracking-[-0.05em]">Verificación facial</h2><p className="mt-2 max-w-md text-sm leading-6 text-muted-foreground">Comprobamos que eres una persona real. Tu rostro se procesa solo en este prototipo y no se guarda.</p></div>{verified && <BadgeCheck className="size-7 text-accent" />}</div><div className="mt-7 rounded-2xl border border-dashed border-border bg-muted/50 p-6 text-center">{scanState === 'scanning' ? <><div className="mx-auto flex size-20 animate-pulse items-center justify-center rounded-full border-4 border-accent bg-accent/10"><ScanFace className="size-9 text-accent" /></div><p className="mt-4 font-medium">Analizando tu rostro…</p><p className="mt-1 text-xs text-muted-foreground">Mantén la mirada al frente</p></> : scanState === 'done' ? <><div className="mx-auto flex size-20 items-center justify-center rounded-full bg-accent text-accent-foreground"><Check className="size-9" /></div><p className="mt-4 font-medium">Identidad verificada</p><p className="mt-1 text-xs text-muted-foreground">Ya puedes reservar y publicar espacios.</p></> : <><Camera className="mx-auto size-9 text-muted-foreground" /><p className="mt-4 font-medium">Listo para comenzar</p><p className="mt-1 text-xs text-muted-foreground">Te tomará menos de un minuto.</p></>}</div>{!verified && <button disabled={scanState === 'scanning'} onClick={startScan} className="mt-5 flex w-full items-center justify-center gap-2 rounded-full bg-primary px-5 py-3 text-sm font-medium text-primary-foreground disabled:opacity-50"><ScanFace className="size-4" /> {scanState === 'scanning' ? 'Verificando…' : 'Comenzar verificación'}</button>}<div className="mt-5 flex gap-2 text-xs text-muted-foreground"><LockKeyhole className="size-4 shrink-0" /> Sin fotos guardadas. Esta simulación no usa cámara real.</div></div></div></PageShell>
}
